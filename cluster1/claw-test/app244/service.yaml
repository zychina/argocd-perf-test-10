apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app244-svc
  namespace: clawtest
  labels:
    app: perf-test10-app244
spec:
  selector:
    app: perf-test10-app244
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
