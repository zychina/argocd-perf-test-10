apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app150-svc
  namespace: clawtest
  labels:
    app: perf-test10-app150
spec:
  selector:
    app: perf-test10-app150
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
