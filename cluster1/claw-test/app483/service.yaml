apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app483-svc
  namespace: clawtest
  labels:
    app: perf-test10-app483
spec:
  selector:
    app: perf-test10-app483
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
