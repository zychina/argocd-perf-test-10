apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app828-svc
  namespace: clawtest
  labels:
    app: perf-test10-app828
spec:
  selector:
    app: perf-test10-app828
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
