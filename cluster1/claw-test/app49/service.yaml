apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app49-svc
  namespace: clawtest
  labels:
    app: perf-test10-app49
spec:
  selector:
    app: perf-test10-app49
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
