apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app294-svc
  namespace: clawtest
  labels:
    app: perf-test10-app294
spec:
  selector:
    app: perf-test10-app294
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
