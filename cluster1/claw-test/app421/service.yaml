apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app421-svc
  namespace: clawtest
  labels:
    app: perf-test10-app421
spec:
  selector:
    app: perf-test10-app421
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
