apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app971-svc
  namespace: clawtest
  labels:
    app: perf-test10-app971
spec:
  selector:
    app: perf-test10-app971
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
