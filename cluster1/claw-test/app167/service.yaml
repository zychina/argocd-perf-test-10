apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app167-svc
  namespace: clawtest
  labels:
    app: perf-test10-app167
spec:
  selector:
    app: perf-test10-app167
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
