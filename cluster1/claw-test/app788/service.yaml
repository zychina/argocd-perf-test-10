apiVersion: v1
kind: Service
metadata:
  name: perf-test10-app788-svc
  namespace: clawtest
  labels:
    app: perf-test10-app788
spec:
  selector:
    app: perf-test10-app788
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
